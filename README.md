## Тестовое задание для команды QTIM

### 🚀 Запуск проекта

Установите зависимости проекта с помощью команды:

```bash
yarn
```

Для запуска проекта в режиме разработки воспользуйтесь командой:

```bash
yarn start:dev
```

Для запуска проекта в docker контейнере используйте команду:

```bash
docker-compose up -d
```

Swagger UI доступен по [адресу](http://localhost:3001/swagger)

### ✅ Запуск тестов

Для запуска тестов используйте команду:

```bash
yarn test
```

Для запуска тестов с отчетом coverage используйте команду:

```bash
yarn test:cov
```

Отчет можно найти в этой [директории](coverage/)

Для запуска тестов в режиме `watch` используйте команду:

```bash
yarn test:watch
```

### 🛠️ Миграции

Для создания миграций с помощью TypeOrm используйте команду:

```bash
yarn migrations
```

Это создаст новые миграции в [директории](src/migrations/). Вы можете при необходимости переименовать файлы миграций по своему усмотрению.
Также можно создать миграцию вручную в той же директории.
Миграции автоматически применяются при старте приложения.

### Кеширование

Ответ на запрос `GET /api/articles/{articleId}` кешируется на 60 секунд.
При выполнении запросов `PATCH /api/articles/{articleId}` и `DELETE /api/articles/{articleId}` кеш для соответствующей статьи инвалидируется.

### 🛡️🍪 Jwt и cookie

Вызывая ендпоинт `/api/auth/signin` пользователь получает две `cookie`:

- access-token
- refresh-token

Обе куки имеют флаг `httpOnly: true`, так что достать их на стороне клиента не получится. Передавать руками в заголовки запросов их не надо, они автоматически будут прикреплены и отправлены в нужные запросы.
